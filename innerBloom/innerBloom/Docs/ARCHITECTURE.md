# innerBloom 项目架构

> 采用 **MVVM + Clean Architecture** 分层架构，便于维护和扩展

---

## 目录结构

```
innerBloom/
│
├── App/                        # 🚀 应用入口
│   └── innerBloomApp.swift
│
├── Models/                     # 📦 数据模型层
│   ├── DiaryEntry.swift        # 日记条目 + ChatMessage
│   └── Tag.swift               # 标签模型
│
├── Views/                      # 🎨 视图层 (UI)
│   ├── Main/                   # 主页相关
│   │   ├── ContentView.swift   # 主视图容器
│   │   ├── DiaryListView.swift # 日记列表
│   │   └── TagChipsView.swift  # 标签图块
│   │
│   ├── Chat/                   # 聊天相关
│   │   ├── ChatView.swift      # 完整聊天视图
│   │   ├── ChatOverlayView.swift # 聊天覆盖层
│   │   └── InputAreaView.swift # 输入区域
│   │
│   ├── Media/                  # 媒体相关
│   │   ├── MediaAreaView.swift # 媒体区域
│   │   └── CircleImageView.swift    # 圆形图片视图
│   │
│   └── Components/             # 通用组件
│       └── ActionButtonsView.swift # 操作按钮
│
├── ViewModels/                 # 🧠 视图模型层
│   └── HomeViewModel.swift     # 主页视图模型
│
├── Services/                   # ⚙️ 服务层
│   ├── Local/                  # 本地服务
│   │   ├── LocalDraftManager.swift   # 草稿管理
│   │   ├── LocalMediaManager.swift   # 媒体管理
│   │   └── SpeechRecognizer.swift    # 语音识别
│   │
│   ├── Network/                # 网络服务
│   │   ├── NetworkMonitor.swift      # 网络监控
│   │   ├── SupabaseConfig.swift      # Supabase 配置
│   │   ├── SupabaseDatabaseService.swift # 数据库服务
│   │   └── SupabaseStorageService.swift  # 存储服务
│   │
│   └── AI/                     # AI 服务（预留）
│       └── .gitkeep
│
├── Design/                     # 🎭 设计系统
│   └── Theme.swift             # 主题配置
│
├── Resources/                  # 📁 资源文件
│   ├── Assets.xcassets/        # 图片资源
│   └── Supabase.plist          # Supabase 配置文件
│
└── Docs/                       # 📚 文档
    └── implementation.md       # 需求文档
```

---

## 层级职责

| 层级 | 职责 | 依赖方向 |
|------|------|----------|
| **Views** | UI 展示、用户交互 | → ViewModels |
| **ViewModels** | 业务逻辑、状态管理 | → Services, Models |
| **Services** | 数据获取、外部服务 | → Models |
| **Models** | 数据结构定义 | 无依赖 |

---

## 数据流

```
┌─────────┐    ┌─────────────┐    ┌──────────┐    ┌────────┐
│  Views  │ ←→ │ ViewModels  │ ←→ │ Services │ ←→ │ Models │
└─────────┘    └─────────────┘    └──────────┘    └────────┘
     ↑                                   ↓
     └───────────── UI 更新 ─────────────┘
```

---

## 命名规范

- **Views**: `*View.swift`
- **ViewModels**: `*ViewModel.swift`
- **Services**: `*Service.swift` / `*Manager.swift`
- **Models**: 实体名称（如 `DiaryEntry.swift`）
